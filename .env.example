# Reference only. Wrangler reads .dev.vars locally; use Cloudflare secrets remotely.
SITE_URL=https://staging.example.invalid
ALLOW_LEGACY_STAFF=false
ZIRAAT_VERIFIED=false
# Only after bank contract/API and hosted payment integration have been verified:
# ZIRAAT_CLIENT_ID=
# ZIRAAT_STORE_KEY=
# ZIRAAT_GATE_URL=https://bank-approved-test-host.invalid/hosted-payment
# D1 binding DB is configured in wrangler.jsonc, not a browser environment variable.
